
function draw(){
    background("green");
}
function setup(){
canvas=createCanvas(400,300);
camera=createCapture(VIDEO);
camera.position(100,250);
camera.size(400,300);
canvas.position(600,250);
}

function preload(){

}
